<!DOCTYPE html>
<html>
  <head>
    <title>Orcamentos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      body {
        background-color: #f0f0f0; /* Cor de fundo pastel */
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
      }

      .container {
        max-width: 800px;
        margin: 20px auto;
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }

      h1 {
        margin-bottom: 20px;
        color: #333; /* Cor do texto */
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
      }

      th, td {
        padding: 10px;
        text-align: left;
        border-bottom: 1px solid #ddd;
      }

      th {
        background-color: #f2f2f2; /* Cor de fundo da linha do cabeçalho */
      }

      .btn {
        display: inline-block;
        padding: 10px 20px;
        margin-bottom: 10px;
        text-decoration: none;
        color: #fff; /* Cor do texto */
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      .btn-primary {
        background-color: #ff9999; /* Azul */
      }

      .btn-primary:hover {
        background-color: #ff6666; /* Azul escuro */
      }

      .btn-success {
        background-color: #28a745; /* Verde */
      }

      .btn-success:hover {
        background-color: #218838; /* Verde escuro */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Orcamentos</h1>
      <%= link_to 'Home', root_path, class: "btn btn-primary" %>
      <table>
        <thead>
          <tr>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Quantidade</th>
            <th>Valor</th>
            <th colspan="1"></th>
          </tr>
        </thead>
        <tbody>
          <% @orcamentos.each do |orcamento| %>
            <tr>
              <td><%= orcamento.nome %></td>
              <td><%= orcamento.descricao %></td>
              <td><%= orcamento.quantidade %></td>
              <td><%= orcamento.valor %></td>
              <td><%= link_to 'Ver', orcamento, class: "btn btn-primary" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <br>
      <%= link_to 'Novo Orcamento', new_orcamento_path, class: "btn btn-success" %>
    </div>
  </body>
</html>
